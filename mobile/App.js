import React from 'react';
import {SafeAreaView } from 'react-native';
import MainScreen from './src/screens/MainScreen'; // Ajuste o caminho, se necessário

export default function App() {
  return (
    <SafeAreaView>
      <MainScreen />
    </SafeAreaView>
  );
}
